<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Med Match - Authentication</title>
<script src="https://www.gstatic.com/firebasejs/8.10.0/firebase-app.js"></script>
<script src="https://www.gstatic.com/firebasejs/8.10.0/firebase-auth.js"></script>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
:root {
  --primary: #58c4c8;
  --secondary: #5BA4E6;
  --accent: #FF6B6B;
  --deep-blue: #2A3B4C;
}

body {
  background: linear-gradient(135deg, var(--primary), var(--secondary));
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'Poppins', sans-serif;
}

.auth-container {
  background: rgba(255, 255, 255, 0.98);
  padding: 2.5rem;
  border-radius: 30px;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  width: 100%;
  max-width: 440px;
  transform-style: preserve-3d;
  position: relative;
  overflow: hidden;
}

.auth-container::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: conic-gradient(
    from 180deg,
    var(--primary),
    var(--secondary),
    var(--accent),
    var(--secondary),
    var(--primary)
  );
  animation: rotate 6s linear infinite;
  z-index: -1;
  opacity: 0.1;
}

@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.form-header {
  text-align: center;
  margin-bottom: 2rem;
}

.form-header h1 {
  color: var(--deep-blue);
  font-size: 2.2rem;
  margin-bottom: 0.5rem;
  font-weight: 700;
  position: relative;
  display: inline-block;
}

.form-header h1::after {
  content: '';
  position: absolute;
  bottom: -8px;
  left: 50%;
  transform: translateX(-50%);
  width: 60px;
  height: 3px;
  background: var(--accent);
  border-radius: 2px;
}

.user-type-selector {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
  margin-bottom: 2rem;
}

.user-type-card {
  padding: 1.5rem;
  border: 2px solid #e0e0e0;
  border-radius: 15px;
  cursor: pointer;
  transition: all 0.3s ease;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.user-type-card:hover {
  transform: translateY(-3px);
  border-color: var(--primary);
}

.user-type-card.selected {
  border-color: var(--accent);
  background: rgba(255, 107, 107, 0.05);
}

.user-type-card.selected::after {
  content: '✓';
  position: absolute;
  top: 8px;
  right: 8px;
  color: var(--accent);
  font-weight: 700;
}

.input-group {
  margin-bottom: 1.5rem;
  position: relative;
}

.input-group input {
  width: 100%;
  padding: 14px 0px;
  border: 2px solid #e0e0e0;
  border-radius: 12px;
  font-size: 1rem;
  transition: all 0.3s ease;
  background: rgba(255, 255, 255, 0.9);
}

.input-group input:focus {
  border-color: var(--primary);
  box-shadow: 0 4px 15px rgba(88, 196, 200, 0.2);
}

.floating-label {
  position: absolute;
  left: 15px;
  top: 50%;
  transform: translateY(-50%);
  color: #888;
  pointer-events: none;
  transition: all 0.3s ease;
}

.input-group input:focus ~ .floating-label,
.input-group input:not(:placeholder-shown) ~ .floating-label {
  top: -10px;
  left: 10px;
  font-size: 0.8rem;
  color: var(--primary);
  background: white;
  padding: 0 5px;
}

.auth-form button {
  width: 100%;
  padding: 16px;
  background: linear-gradient(135deg, var(--accent), #ff8787);
  color: white;
  border: none;
  border-radius: 12px;
  font-size: 1.1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.auth-form button:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(255, 107, 107, 0.3);
}

.auth-form button::after {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: linear-gradient(
    45deg,
    transparent,
    rgba(255,255,255,0.3),
    transparent
  );
  transform: rotate(45deg);
  animation: shine 3s infinite;
}

@keyframes shine {
  0% { left: -50%; }
  100% { left: 150%; }
}

.google-auth {
  background: var(--deep-blue) !important;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 14px;
  border-radius: 12px;
  transition: all 0.3s ease;
}

.google-auth:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(42, 59, 76, 0.2);
}

.password-strength {
  height: 4px;
  background: #eee;
  border-radius: 2px;
  margin-top: 8px;
  overflow: hidden;
  position: relative;
}

.strength-bar {
  height: 100%;
  width: 0;
  background: var(--accent);
  transition: width 0.3s ease;
}

.toggle-form {
  text-align: center;
  margin-top: 1.5rem;
  color: var(--deep-blue);
}

.toggle-form a {
  color: var(--accent);
  font-weight: 600;
  position: relative;
}

.toggle-form a::after {
  content: '';
  position: absolute;
  bottom: -2px;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--accent);
  transition: width 0.3s ease;
}

.toggle-form a:hover::after {
  width: 100%;
}

.loading-spinner {
  display: none;
  width: 24px;
  height: 24px;
  border: 3px solid rgba(255,255,255,0.3);
  border-radius: 50%;
  border-top-color: white;
  animation: spin 1s ease infinite;
  margin: 0 auto;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
</style>
</head>
<body>
<div class="auth-container">
  <div class="form-header">
    <h1>Med Match</h1>
    <p>Your Gateway to Safe Medication Management</p>
  </div>

  <div class="user-type-selector">
    <div class="user-type-card" onclick="selectUserType(this, 'patient')">
      <h3>Patient</h3>
      <p>Access personalized insights</p>
    </div>
    <div class="user-type-card" onclick="selectUserType(this, 'doctor')">
      <h3>Healthcare Pro</h3>
      <p>Advanced tools & analytics</p>
    </div>
  </div>

  <form class="auth-form" id="loginForm">
    <div class="input-group">
      <input type="email" id="email" placeholder=" " required />
      <label class="floating-label">Email Address</label>
    </div>
    
    <div class="input-group">
      <input type="password" id="password" placeholder=" " required />
      <label class="floating-label">Password</label>
      <div class="password-strength">
        <div class="strength-bar"></div>
      </div>
    </div>

    <div class="error-message" id="errorMessage"></div>
    <button type="submit">
      <span class="button-text">Secure Sign In</span>
      <div class="loading-spinner"></div>
    </button>
  </form>

  <div class="toggle-form">
    New to Med Match? <a id="toggleSignUp" href="#">Create Secure Account</a>
  </div>
</div>

<script>
// Enhanced User Type Selection
function selectUserType(element, type) {
  document.querySelectorAll('.user-type-card').forEach(card => {
    card.classList.remove('selected');
  });
  element.classList.add('selected');
  // Add user type to form data
}

// Dynamic Password Strength
document.getElementById('password').addEventListener('input', function(e) {
  const strength = calculatePasswordStrength(e.target.value);
  document.querySelector('.strength-bar').style.width = `${strength}%`;
});

function calculatePasswordStrength(password) {
  let strength = 0;
  if (password.length >= 8) strength += 30;
  if (password.match(/[A-Z]/)) strength += 20;
  if (password.match(/[0-9]/)) strength += 20;
  if (password.match(/[^A-Za-z0-9]/)) strength += 30;
  return Math.min(strength, 100);
}

// Animated Form Toggle
document.getElementById('toggleSignUp').addEventListener('click', (e) => {
  e.preventDefault();
  const form = document.getElementById('loginForm');
  form.classList.add('fade-out');
  
  setTimeout(() => {
    form.innerHTML = `
      <div class="input-group">
        <input type="text" id="name" placeholder=" " required>
        <label class="floating-label">Full Name</label>
      </div>
      <div class="input-group">
        <input type="email" id="email" placeholder=" " required>
        <label class="floating-label">Professional Email</label>
      </div>
      <div class="input-group">
        <input type="password" id="password" placeholder=" " required>
        <label class="floating-label">Create Password</label>
        <div class="password-strength">
          <div class="strength-bar"></div>
        </div>
      </div>
      <div class="error-message" id="errorMessage"></div>
      <button type="submit">
        <span class="button-text">Create Secure Account</span>
        <div class="loading-spinner"></div>
      </button>
    `;
    form.classList.remove('fade-out');
    document.querySelector('.toggle-form').innerHTML = 
      'Already registered? <a id="toggleSignIn" href="#">Access Secure Portal</a>';
  }, 300);
});

// Add loading state
document.querySelector('form').addEventListener('submit', function() {
  this.querySelector('.button-text').style.display = 'none';
  this.querySelector('.loading-spinner').style.display = 'block';
});

// Rest of Firebase logic remains similar but with additional user type handling
</script>
</body>
</html>
